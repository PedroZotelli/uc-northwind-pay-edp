{
  "name": "ebook-converge-build",
  "version": "0.1.0",
  "private": true,
  "description": "Build harness for ebooks/ebook-converge.pdf (page-per-slide HTML rasterized via Chrome).",
  "type": "commonjs",
  "dependencies": {
    "mermaid": "^11.12.0",
    "puppeteer-core": "^24.0.0"
  }
}
