version: 2

sources:
  - name: landing
    schema: landing
    tables:
      - name: type01_card_settlement
        description: dlt-registered immutable landing Parquet (view over modern/landing).
        columns:
          - name: batch_id
            tests: [not_null]
          - name: source_record_number
            tests: [not_null]
