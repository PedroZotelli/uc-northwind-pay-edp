version: 2

models:
  - name: type01_gold
    description: Type 01 Gold. Grain batch_id + currency. Not a Postgres copy.
    columns:
      - name: batch_id
        tests: [not_null]
      - name: currency
        tests: [not_null]
      - name: applied_net_amount
        tests: [not_null]
      - name: status
        tests: [not_null]
