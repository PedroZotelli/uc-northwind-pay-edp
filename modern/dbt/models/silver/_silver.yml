version: 2

models:
  - name: type01_silver
    description: Type 01 Silver. Record grain batch_id + source_record_number. Currency BRL.
    columns:
      - name: batch_id
        tests: [not_null]
      - name: source_record_number
        tests: [not_null]
      - name: currency
        tests: [not_null]
