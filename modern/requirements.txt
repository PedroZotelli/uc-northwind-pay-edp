# Modern pipeline runtime, pinned for deterministic local rebuilds.
#
# Deliberately separate from legacy/runner/requirements.txt. The legacy
# baseline is a frozen oracle and the modern implementation must stay an
# independent second implementation, so the two environments never share a
# resolver. Install with:
#
#   python3.12 -m venv modern/.venv
#   modern/.venv/bin/pip install -r modern/requirements.txt

pyarrow==25.0.0
duckdb==1.5.5
dlt[duckdb]==1.29.1
dbt-core==1.12.0
dbt-duckdb==1.10.1
dagster==1.13.15
dagster-webserver==1.13.15
fastapi==0.140.0
uvicorn==0.51.0
pydantic==2.13.4
mypy==2.3.0
PyYAML==6.0.3
psycopg[binary]==3.3.4
