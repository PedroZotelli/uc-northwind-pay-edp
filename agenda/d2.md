# Day 2 — Translator

**Seat:** Translator (SWE).  
**Converge:** Pass **2–4** (Structure → Decompose → Consensus — the barrier), then Pass **5–8** on the **ingest → landing** seam. Bind is the fence.  
**This file** is the brief for the Day 2 PPT, the ADRs, the sign-off, the first modern write, and the Type `01` landing gate.  
Keep it short on stage. Build slides from the chapter list. Do not invent extra story.

**Swing:** Stage (why a translator / first write) → Craft (ADRs from the brain + graph, then Task-Spec + Bind) → Floor (sign Consensus, then Type `01` to Parquet) → Debrief (landing is closed; lakehouse waits).

Public gate: *legacy translated safely, running isolated.*  
Rings tonight: **harness.** HITL strong. You validate everything.

---

## Intent

Yesterday: plant MATCHED, Second Brain (nine packs at [`brain/notebooklm/`](../brain/notebooklm/README.md) — query it, do not rebuild), OntoLayer, Capture + Intent. **Nobody signed Consensus.**  
Today the room structures what is true, cuts the seams, signs the barrier, then translates Type `01` into an independent plant that reads the **same raw bytes** and writes **Parquet to `modern/landing/`**. They leave able to:

1. Write grounding ADRs from OntoLayer facts + Second Brain / transcripts — what is true, never how to build (Pass 2).
2. Cut the genuine seams, name the steel thread Type `01` (Pass 3).
3. Run Consensus as a different voice. Owner signs. **That is the barrier** (Pass 4).
4. Author eval-backed Task-Specs for ingest → landing. No eval, no build.
5. Bind the runtime: paths and privileges, not "please don't."
6. Ship the Type `01` five-file package (`model → parser → schema → writer → handler`).
7. Publish deterministic sanitized Parquet **only** for admissible batches. Refuse `DF-SOURCE-*` and malformed the same way legacy does.

They do not start the parser before the owner signs. They do not stand up dlt, dbt, or Dagster. They do not serve Gold. They do not open Type `06`. They do not edit `legacy/` or `contracts/`. They do not write Parquet back to SFTP.

---

## Stage — chapters for the PPT

Use this order. One idea per slide.

### 1. The barrier is still in front of you

- Day 1 stopped at Intent. ADRs, seams, and Consensus are **this morning**.
- OntoLayer and the Second Brain supply facts. They do not replace a signed ADR.
- If the tech-spec is missing, stop and reopen Day 1. Do not invent the brief in the parser.
- After the owner signs, the machine builds what was signed. Not before.

### 2. The translator

- SWE seat. Grammar, privacy, money, and the first write.
- You do not replace Java. You sit beside it on the same `raw/incoming`.
- Independence: no Java import, no procedure reuse, no CSV-as-input.

### 3. The first write is not SFTP

```text
same SFTP raw/incoming
  → Python (model → parser → schema → writer → handler)
  → modern/landing/ Parquet + readiness manifest
```

Java writes CSV to `csv/outgoing`. Python writes Parquet to landing. Mixing those two destinations is a failed day.

### 4. The five-file package (draw this)

```text
modern/ingestion/src/northwind_pay/types/<number-name>/
├── model.py      typed records, exact Decimal
├── parser.py     transport, positions, grammar, encoding, signs
├── schema.py     validation, privacy-safe fields, controls
├── writer.py     deterministic atomic Parquet + metadata
└── handler.py    composes the four for one batch
```

Shared libraries may own checksums, idempotency, quarantine, provenance. Type grammar, privacy, rounding, and reconciliation stay inside the numbered package.

### 5. Converge today

```text
2 Structure → ADRs vs reality. OntoLayer + Second Brain. What is true, never how.
3 Decompose → seam → swimlane → leg. Steel thread = Type 01.
4 Consensus → different-family adversary. Owner signs. THE BARRIER.
──────────────────────── barrier ────────────────────────
5 Tasking   → one leaf, one eval, signed_off starts false
  Register  → opt-in. 1 spec = 1 issue. Not required to move.
7 Bind      → epoch + allowed paths + guards + worker brief
8 Loop      → attempt → eval → learn → repeat. Eval is done.
```

No parser before Pass 4. Bind must refuse writes to `legacy/`, `contracts/`, `gen/`, `infra/`. A polite system prompt is not a fence.

### Suggested seams (confirm on the floor; do not invent extras)

| Seam | Owner seat | Built on |
|---|---|---|
| Ingest → landing | Translator (today, after the barrier) | same raw SFTP → five-file package → `modern/landing/` Parquet |
| dlt → Gold | Constructor (Day 3) | register-only dlt → DuckLake/DuckDB → dbt B/S/G → golden-match |
| Orchestrate + serve | Orchestrator (Day 4) | Dagster, evidence, read-only Gold |

Steel thread: Type `01` valid-minimal through those three arrows.

### The ten questions that are still open

These live in [`plans/modern.md`](../plans/modern.md). Close them as ADRs tonight, or they are not closed:

1. Python version, packaging, validation libraries.
2. Canonical Parquet schema, compression, ordering, partition, metadata.
3. Exact dlt role (register only — already the doctrine; write it).
4. DuckLake storage and catalog placement.
5. Bronze, Silver, Gold grains and keys.
6. What lives in the five-file package vs what lives in dbt.
7. Record and aggregate keys for golden-match.
8. Dagster asset, partition, retry, backfill model.
9. First read-only FastAPI surface and narrow MCP tools.
10. CI — default remains **out of scope**.

### 6. Type `01` is the steel thread

- Transport: ISO-8859-1 fixed width, COBOL overpunch, LF.
- Privacy: PAN (token + last4), CPF (`*******` + last4).
- Source lie: net declared `173.44` → computed `173.45` → `SOURCE_CONTROL_TOTAL_MISMATCH`.
- Five canonical outcomes + replay + immutable conflict. See [`plans/legacy.md`](../plans/legacy.md).

Stretch, only after Type `01` landing is closed: start Types `02`–`04` packages. Do not empty-scaffold Type `05` or `06`.

---

## Craft — kits on this day

| Kit | Job today |
|---|---|
| **Converge** | Pass 2–4 until the owner signs, then 5–8 on one seam. Still does not write product code. |
| **Seamwise** | Cut the ingest → landing lane at Decompose. Do not invent a fourth estate. |
| **Task-Spec** | Leaves with runnable evals — **after** Consensus. The loop stops on eval, not on attempt-count. |
| **Brief-Spec** | Morning is decide / review. Afternoon is implement. |

### Agent rules today

| Rule | Why |
|---|---|
| No product code before Consensus | The barrier is still this morning |
| Bind is the fence | Paths and privileges, not vendor loyalty |
| Any agent that can honor Bind may sit here | We grade gates, not IDEs |
| Decimal, never float | One cent is the whole class |
| No Parquet on reject | Expected terminal, not missing data |
| Do not read Java to compute a result | Observation only |

---

## Floor — live work (instructor + tables)

### First half — sign the barrier

1. Open yesterday's tech-spec, the **same** Second Brain (`NorthWind Pay · estate`, nine sources — types `01`–`05`), and OntoLayer. Draft the first ADR in the room (example: "dlt registers landing; it does not re-parse"). Facts + constraints, no build steps. Do not rebuild the notebook. Do not add Type `06`.
2. Walk one contradiction in public: estate mail vs Type `01` inbound vs `contracts/` vs the MATCHED batch. Keep the `173.44` lie.
3. Cut the three seams. Name the steel thread Type `01`. Time-box. Do not start Task-Specs yet.
4. Run Consensus as a different voice (another table, or a second agent family). Every objection is FIXED or ACCEPTED.
5. Owner signs. **Stop the design.** If they will not sign, there is no afternoon parser.

### Second half — first write

6. Author the first Type `01` leaf in public (parser or writer — one leaf, one eval).
7. Bind a volunteer agent. Ask it to touch `legacy/processor/`. It must fail closed.
8. Loop Type `01` `valid-minimal` until landing Parquet + readiness manifest exist and are deterministic on replay.
9. Run `DF-SOURCE-001`. Confirm: refuse, declaration `173.44` preserved, computed `173.45`, **zero** Parquet, peers may continue.
10. Run malformed. Same shape: quarantine, no publication.
11. If and only if the Type `01` landing gate is green: tables may open Type `02` or `03` inbound and start the same five-file shape.

Do not start dlt. Do not open a DuckDB catalog. Do not walk Type `05` rounding.

---

## Debrief

- Design is done when the owner signs, not when the slide looks complete.
- Landing is a plant, not a folder with a parquet in it. Replay and reject are part of the gate.
- Tomorrow the constructor registers what you wrote. If the Parquet schema is sloppy, Day 3 inherits the slop.
- The agent is a seat. Bind is the law. The eval is the judge.

---

## Homework

1. Re-read the signed brief, the ADRs, and the seam list. Restate them without the file open.
2. Type `01` landing replay is identical. If it drifted, fix it before sleep.
3. Skim Types `02`–`04` inbound **layouts only** (already in the Day 1 notebook, packs 04–06). Do not start Type `05`.
4. Read the golden-match two questions in [`plans/modern.md`](../plans/modern.md). You attach them tomorrow, you do not implement the referee.

---

## Research

Query tonight for the constructor's morning. Same notebook as Day 1
([`brain/notebooklm/`](../brain/notebooklm/README.md)). Do not rebuild it.

1. **OntoLayer** — "What grain and keys must dlt register, and what is the
   reconciliation grain?"
2. **Second Brain** — "What is the first write, and what must it not be?"
3. **plans/modern.md** — read the golden-match two questions. Tomorrow you
   attach them; you do not rewrite the referee.

---

## Exit criteria

- [ ] Pass 2 ADRs exist (ten modern questions closed or parked with an owner). OntoLayer/Second Brain cited as evidence, not as the ADR.
- [ ] Pass 3 seams match Days 3–4. Steel thread is Type `01`.
- [ ] Pass 4 Consensus signed. Every objection FIXED or ACCEPTED.
- [ ] Pass 5–8 ran on ingest → landing. Bind refused frozen paths.
- [ ] Type `01` five-file package exists. `valid-minimal` publishes deterministic Parquet under `modern/landing/`.
- [ ] Rejected and source-defect batches produce no Parquet and no business mutation.
- [ ] First write is landing, not SFTP.
- [ ] No dlt / dbt / Dagster / FastAPI required today.
- [ ] Type `06` still absent.

## Day 2 is not

- Not Day 1 (do not rebuild the Second Brain or re-index OntoLayer unless they are actually down).
- Not "parser first, ADRs later."
- Not Day 3 (no lakehouse, no Gold, no golden-match attach).
- Not "we all agree, ship it" without an adversary pass.
- Not a repair of `173.44`.
- Not CSV on SFTP as a modern input.
