# Pass 8 — The Loop

**Altitude:** runtime. This pass does not lower. It closes.

- Enters: a ready issue / Task-Spec.
- Exits: green eval → pull request. Red feeds the failure back.
- Gate: the eval passed. Attempt count is not a gate.

Notes from live runs land here. The code itself lands on a branch, not
in this folder.
