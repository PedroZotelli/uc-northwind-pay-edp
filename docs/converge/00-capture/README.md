# Pass 0 — Capture (optional)

**Altitude:** idea. **Skip this** when a brief already exists.

- Enters: a raw idea, transcripts, loose notes.
- Exits: a signed BRD, or a written no-go.
- Gate: Pass 1 must not consume an unsigned brief.

Put the BRD in this folder when the room writes one. Leave it empty if
the week starts from the NorthWind Pay case that already has a brief.
