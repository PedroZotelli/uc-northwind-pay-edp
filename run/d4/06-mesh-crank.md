# 06 · Pass 7–8 — crank one leaf

- Slide: Execute 06–07 (Hands-On **slice c · loop**) — tile 06
- Slice: **C · Loop**
- Who: instructor cranks **one** signed leaf in public, then seats
- Next: [`07-packet-dagster.md`](07-packet-dagster.md) on the **same board**

Task-Mesh Show already ran. Mesh **runs** the leaf. It does not author it. Watch the **eval**, not the keystrokes. Bind still on.

## Prompt (verbatim)

```text
Pass 8 Loop on NorthWind Pay. Crank exactly one signed Type 01 leaf first (or the highest ready remaining leaf if Type 01 Gold exists).

Rules:
- Bind still on. Frozen: legacy/, contracts/, gen/, infra/.
- Unsigned leaves stay dark.
- Eval is the judge. Chat is not.
- Cross-family engines only if cvg doctor is green.
- Record a packet: leaf id, attempt, eval command, exit, classification or skip, paths under evidence/ (terminal, not Git).

Do not settle if the packet is missing.
Do not rewrite golden_match.py.
Do not start Type 05 on this tile.
```

## Proof

One leaf attempted. Eval ran. A **packet** exists under `evidence/` (or the fail is named). Linear did not move without it.

## If fail

Eval red → retry or stop. Do not “fix expected.” Frozen write → fail closed. No packet → **do not settle**.
