# 05 · Pass 6 — Register + Linear

- Slide: Execute 03–05 (Hands-On **slice b · generate**) — tile 05
- Slice: **B · Generate**
- Who: instructor first, then every seat
- Next: Task-Mesh · Linear Show, then [`06-mesh-crank.md`](06-mesh-crank.md) on Execute 06–07

Pass 6 so the mesh can **see** the queue. Linear is the **board**, not the judge. The eval is the judge.

Host, if not done on Craft: pin Task-Spec **3.8.x**. `cvg init`. Project **this** plant’s seams only.

## Prompt (verbatim)

```text
Pass 6 Register on NorthWind Pay.

1. Confirm cvg/ exists (cvg init) or say the agent will keep writing docs/.
2. Project this plant’s seams only: ingest-landing, dlt-gold, orchestrate-serve.
   Do not copy assurance / foundation / models.
3. List docs/tasks/ leaves that are ready (have evals, signed_off false).
4. Put that queue on Linear (or a visible board if Linear is down). One card per leaf.
   Linear moves when an eval settles — not when chat says done.

Do not crank yet.
Do not settle a leaf without a packet.
```

## Proof

Queue is visible (Linear or named fallback). Seams are **this** plant’s names. Every card points at a leaf with an eval.

## If fail

Foreign lane names → delete and re-project. `cvg` errors → agent still writes `docs/`; still put the queue on a board. No evals → **dark 06–09**.
