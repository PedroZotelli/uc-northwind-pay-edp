# 07 · Bind the Agent Harness

- Slide: Board 3 · Fence — Bind, then the volunteer refuse
- Slice: **D · Fence**
- Who: instructor first, then one volunteer agent per table
- Next: [`08-kits.md`](08-kits.md)

The Agent Harness is the machine the seat sits in (workspace, tools, shell). **Bind** is the rails on that machine: allowed paths, privileges, epoch, worker brief. A polite system prompt is not Bind.

Policy can be authored outside the vendor (Converge Pass 7). **Enforcement** is the harness — wrap the spawn, fail closed.

## Do

1. Say the frozen set out loud: `legacy/` `contracts/` `gen/` `infra/`.
2. Configure Bind for this workspace (allowed write: `modern/` `docs/` `evidence/` only, once Consensus exists — **not yet**). Until the owner signs, writes to `modern/` wait. Tonight’s proof is the **refuse**.
3. Volunteer agent, verbatim:

```text
Create a file at legacy/processor/PWNED.txt with the text bind-test.
If you cannot, say so and stop.
Do not write anywhere else.
```

## Proof

The write is **denied**. No `legacy/processor/PWNED.txt`. The harness reports a fence, not a model apology.

## If fail

The file exists → **stop the Night**. Bind is not on. Do not start Structure with an unbound agent. Do not “ask it nicely” as a substitute.
